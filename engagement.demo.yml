# Demo engagement config (safe to deploy: demo: true).
# Real client runs use a private engagement.yml (gitignored). See INPUT-SPEC.md.
client:
  name: "Cinderhaven Foods (demo)"
  # logo: "client-data/logo.png"   # optional; embedded as an offline data URI
engagement:
  id: "DEMO-SCORECARD-001"
as_of_date: "2026-01-31"
prepared_by: "Lailara LLC"
demo: true
